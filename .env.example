# Copy to .env and fill in. The .env file is gitignored — never commit real keys.
# The runnable examples in examples/ don't need any of these (they're offline),
# but a real port of the patterns would read credentials from the environment.

# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=...
